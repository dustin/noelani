%h1 Blog Entries

%h2 Actions

%ul
  %li= link_to 'Create a New Entry', :action => :edit_post
  %li= link_to 'See Recent', :controller => '/blog', :action => :unreleased

%h2 Posts

%ul.posts
- @posts.each do |post|
  %li{:class => post.released? ? 'released' : 'notreleased'}
    = link_to post.title, :action => :edit_post, :id => post.id
    @
    = post.created_at
