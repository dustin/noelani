.blog_post
  %h2{:class => @post.released? ? 'released' : 'notreleased' }= @post.title
  .content
    = @post.format == 'markdown' ? auto_link(markdown(@post.content)) : @post.content
  .when
    Posted
    = @post.updated_at
%hr